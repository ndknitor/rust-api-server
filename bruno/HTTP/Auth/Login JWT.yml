info:
  name: Login JWT
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8080/api/v1/auth/jwt
  body:
    type: json
    data: |-
      {
        "username" : "admin",
        "password" : "admin"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
